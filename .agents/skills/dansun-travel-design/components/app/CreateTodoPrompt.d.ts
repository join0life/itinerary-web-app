/** Muted "composer" row that opens the event modal, at the top of the 일정 screen. */
export interface CreateTodoPromptProps { label?: string; onClick?: () => void }
export declare function CreateTodoPrompt(props: CreateTodoPromptProps): JSX.Element;
