The product's only icon source is Lucide; use this wrapper instead of pasting SVG.

```jsx
<Icon name="calendar" size={20} />
<Icon name="plus-circle" size={22} color="var(--brand)" />
```

Names in use: home, folder, list-todo, calendar, plus-circle, search, sun, chevron-left, chevron-right, check, x, trash-2, pencil, loader-circle.
