/** 12px pill for owner names, overflow counts (+2) and statuses. */
export interface BadgeProps { tone?: "neutral" | "brand" | "success" | "outline"; children?: React.ReactNode }
export declare function Badge(props: BadgeProps): JSX.Element;
