Composes an icon (or button) into the same bordered shell as `Input`.

```jsx
<InputGroup>
  <InputGroupAddon><SearchIcon /></InputGroupAddon>
  <InputGroupInput placeholder="키워드 입력..." />
</InputGroup>
```
