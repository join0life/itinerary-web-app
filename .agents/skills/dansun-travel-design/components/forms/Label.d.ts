/** Field label — 13px, weight 500, sits 8px above its control. */
export interface LabelProps { htmlFor?: string; muted?: boolean; children?: React.ReactNode }
export declare function Label(props: LabelProps): JSX.Element;
