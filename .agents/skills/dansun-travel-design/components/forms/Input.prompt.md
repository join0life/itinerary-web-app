A 44px text field; pair with `Label` and, on error, a `HelperText`-style line in `--status-danger`.

```jsx
<Label htmlFor="title">제목</Label>
<Input id="title" placeholder="어디로 갈까요?" />
```

Focus = coral border + `--focus-ring`. Set `invalid` for form errors; `disabled` fills with `--surface-muted`.
