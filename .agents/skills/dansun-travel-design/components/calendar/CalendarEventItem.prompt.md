Absolutely positioned inside a relative hour grid; must be a child of `WeeklyCalendar`.

```jsx
<CalendarEventItem title="한라산 등반" startHour={9} endHour={13} />
```

Coral-tinted fill with a 3px coral left bar; times are tabular.
